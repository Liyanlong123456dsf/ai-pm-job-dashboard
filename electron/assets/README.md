# 图标资源

需要添加以下文件才能打包：

- `icon.ico` (Windows) — 256×256，多尺寸 ICO
- `icon.icns` (macOS) — 512×512，ICNS 格式
- `icon.png` (Linux/通用) — 512×512 PNG

可使用在线工具 https://www.icoconverter.com/ 或 https://cloudconvert.com/png-to-icns 生成。

如果暂时没有图标，`electron-builder` 会使用默认图标，功能不受影响。
